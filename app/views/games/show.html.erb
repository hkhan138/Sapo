<!--<p id="notice"><%= notice %></p>-->

<div class = "hero-unit">
	<h1>Game Setup</h1>
</div>

<%= render 'game_details' %>
<%= render 'team_details' %>


<% if !@game.winnerTeam.present? %>
	<%= link_to 'Add Team', "#{new_team_path}/#{@game.id}"%>
	<% if @game.teamsReady %>
	| <%= link_to 'Start Game', {controller: :games,action: :play,id:@game.id} %>
	<% end %>
<% end %>
